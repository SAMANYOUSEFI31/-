# 🔒 سند و سیاست‌های امنیتی بوشیدو (Security Policy & Contracts)

> **سطح سند:** محرمانه و عملیاتی  
> **مرجعیت:** پروتکل‌های امنیتی، مکانیزم‌های هشینگ و قواعد پروداکشن

---

## ۱. سیاست میانبرهای تستی و ورود سریع (Test Shortcuts & Quick Login Lock)

1. **قفل قطعی در پروداکشن:**
   - در حالت `NODE_ENV=production`، متد `allowTestShortcuts()` خروجی `false` تولید می‌کند، مگر اینکه متغیر محیطی `ALLOW_TEST_SHORTCUTS=true` به صورت صریح و آگاهانه روی سرور ست شده باشد.
   - کلیه میانبرهای دور زدن رمز عبور، شبیه‌سازهای لاگین فوری ادمین و سناریوهای تستی در فرانت‌اند و بک‌اند با این فلگ قفل می‌شوند.

2. **هش کلمه عبور با استاندارد بالا (PBKDF2):**
   - ذخیره‌سازی گذرواژه‌ها با Salt رندوم ۱۶ بایتی کریپتوگرافیک و ۱۰۰,۰۰۰ دور اجرای الگوریتم SHA-512 انجام می‌شود.
   - مقایسه هش‌ها با متد `crypto.timingSafeEqual` برای جلوگیری از حملات زمان‌بندی (Timing Attacks) پیاده‌سازی شده است.

3. **کنترل و ریت لیمیت (Rate Limiting):**
   - تمامی مسیرهای عمومی تحت پوشش محافظت در برابر اسپم و حملات Brute-force قرار دارند.
   - ورود و ثبت‌نام دارای سقف سخت‌گیرانه برای جلوگیری از حدس مکرر رمز عبور است.

---

## ۲. چک‌لیست انتشار عمومی (Pre-Release Security Checklist)

قبل از عمومی کردن دامین یا معرفی به کاربران نهایی:
- [ ] متغیر `ALLOW_TEST_SHORTCUTS` در پنل Vercel/سرور خاموش (`false` یا حذف شده) باشد.
- [ ] متغیر `ENABLE_OTP_DEBUG` خاموش باشد تا کدهای تایید در لاگ کلاینت نمایش داده نشوند.
- [ ] متغیر `JWT_SECRET` با یک رشته امن با طول حداقل ۳۲ کاراکتر مقداردهی شده باشد.
- [ ] در صورت استفاده از دیتابیس ابری، متغیر `DATABASE_URL` به درستی تنظیم و لاگ `/api/health` بررسی شود.
- [ ] کلمه عبور پیش‌فرض مدیر (`SUPER_ADMIN_PASS`) در متغیرهای سرور با یک رمز قوی تغییر کند.
