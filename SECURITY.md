# 🔒 سند و سیاست‌های امنیتی بوشیدو (Security Policy & Environment Separation Contract)

> **سطح سند:** محرمانه و عملیاتی  
> **مرجعیت:** پروتکل‌های امنیتی، مکانیزم‌های تفکیک محیط، قرارداد قابلیت‌های تستی و قواعد پروداکشن

---

## ۱. ماتریس تفکیک محیط‌ها و قرارداد سرور (Environment Capability Matrix)

پروژه بوشیدو از معماری صریح تفکیک محیط برنامه (`APP_ENV`) با پیش‌فرض‌های ایمن مبتنی بر قرارداد قابلیت‌های سرور‌محور (`Server-Authoritative Capabilities`) استفاده می‌کند:

- **محیط توسعه (`APP_ENV=development`):** میانبرهای تستی، لاگین سریع، دیباگ پیامک و شبیه‌ساز پرداخت به صورت پیش‌فرض فعال هستند.
- **محیط آزمون (`APP_ENV=test`):** اجرای کدهای تستی و امکان تعیین ساختارهای ایزوله و شبیه‌سازها.
- **محیط استیجینگ (`APP_ENV=staging`):** میانبرهای تستی و شبیه‌سازها به صورت پیش‌فرض غیرفعال هستند و صرفاً در صورت تنظیم صریح `ALLOW_TEST_SHORTCUTS=true` فعال می‌شوند.
- **محیط پروداکشن عمومی (`APP_ENV=production`):** مصونیت مطلق؛ متغیر `ALLOW_TEST_SHORTCUTS` کاملاً نادیده گرفته شده و کلیه میانبرها، لاگین سریع، دیباگ OTP و شبیه‌سازها به طور قطعی قفل و غیرفعال هستند. مقادیر نامعتبر `APP_ENV` نیز طبق قاعده Fail-Closed رفتار پروداکشن را اعمال می‌کنند.

| قابلیت / دسترسی | توسعه محلی (`development`) | تست خودکار (`test`) | استیجینگ (`staging` + `ALLOW_TEST_SHORTCUTS=true`) | پروداکشن عمومی (`production`) |
| :--- | :---: | :---: | :---: | :---: |
| **ورود سریع (Quick Login)** | ✅ فعال | ✅ فعال | ✅ فعال | ❌ قفل قطعی (HTTP 403) |
| **ارائه‌دهنده پیامک / OTP** | 📦 آداپتور شبیه‌ساز امن (MockSmsProvider) | 📦 آداپتور آزمون | 📦 آداپتور شبیه‌ساز امن | 🛡️ Fail-Closed (بدون لاگ کد؛ در غیاب ارائه‌دهنده، ۵۰۳) |
| **دیباگ کد OTP در خروجی API** | ✅ با `ENABLE_OTP_DEBUG=true` | ✅ با `ENABLE_OTP_DEBUG=true` | ✅ با `ENABLE_OTP_DEBUG=true` | ❌ غیرفعال قطعی (هرگز ارسال نمی‌شود) |
| **درگاه پرداخت اشتراک** | 💳 شبیه‌ساز امن (Simulator Adapter) | 💳 آداپتور آزمون | 💳 شبیه‌ساز امن (Simulator Adapter) | 🛡️ Fail-Closed (رد شبیه‌ساز؛ نیازمند ارائه‌دهنده واقعی) |
| **سید پیش‌فرض کاربر/ادمین** | ✅ فعال | ✅ فعال | ✅ فعال (با میانبر) | ❌ بدون اکانت پیش‌فرض خودکار |
| **الزام کلید امن JWT (حداقل ۳۲ کاراکتر)** | ⚠️ با کلید منعطف توسعه | ⚠️ با کلید منعطف آزمون | 🚨 اجباری (حداقل ۳۲ کاراکتر) | 🚨 اجباری (حداقل ۳۲ کاراکتر) |
| **هدرهای امنیتی (CSP & Frame-Ancestors)** | 🌐 منعطف برای آی‌فریم و پیش‌نمایش | 🌐 منعطف | 🌐 منعطف | 🛡️ هاردن‌شده و بسته (`SAMEORIGIN`) |
| **بررسی سلامت عمومی (`/api/health`)** | 🔍 وضعیت لایونس | 🔍 وضعیت لایونس | 🔍 وضعیت لایونس | 🔍 وضعیت لایونس بدون نشت اطلاعات |
| **دیاگنوستیک کامل سرور (`/api/admin/diagnostics`)** | 🔐 نیازمند توکن معتبر ادمین | 🔐 نیازمند توکن ادمین | 🔐 نیازمند توکن معتبر ادمین | 🔐 نیازمند توکن معتبر ادمین |

---

## ۲. قوانین تجزیه سخت‌گیرانه مقادیر بولی (Strict Boolean Parsing)

متغیرهای محیطی نظیر `ALLOW_TEST_SHORTCUTS`، `ENABLE_OTP_DEBUG` و `ENABLE_QUICK_LOGIN` با تابع اعتبارسنجی سخت‌گیرانه `parseStrictBoolean` ارزیابی می‌شوند:
- صرفاً رشته دقیق `"true"` (بدون حساسیت به حروف بزرگ و با حذف فاصله‌های اضافی) به عنوان مقدار `true` پذیرفته می‌شود.
- مقادیر خالی، `"1"`، `"yes"`، `"false"`، یا هر مقدار متفرقه به عنوان `false` تفسیر شده و سیستم به حالت امن (**Fail-Closed**) می‌رود.
- در محیط پروداکشن (`APP_ENV=production`)، مقدار `ALLOW_TEST_SHORTCUTS` به طور کلی بی‌اثر است و نادیده گرفته می‌شود.

---

## ۳. سیاست میانبرهای تستی و ورود سریع (Test Shortcuts & Quick Login Lock)

1. **مصونیت قطعی پروداکشن (Absolute Public Production Immunity):**
   - در حالت `APP_ENV=production` (یا وقتی `NODE_ENV=production` بدون تعیین `APP_ENV` است)، تابع `allowTestShortcuts()` همواره `false` برمی‌گرداند. حتی اگر `ALLOW_TEST_SHORTCUTS=true` تنظیم شده باشد، هیچ امکانی برای فعال‌سازی میانبرهای تستی در پروداکشن وجود ندارد.
   - کلیه میانبرهای دور زدن رمز عبور، لاگین فوری ادمین و سناریوهای تستی در فرانت‌اند و بک‌اند با این قاعده قفل می‌شوند.

2. **الزام کلید JWT یکپارچه (Single Source of Truth for JWT Secret):**
   - تمام بخش‌های سیستم کلید JWT را منحصراً از تابع `getJwtSecret()` دریافت می‌کنند.
   - در محیط‌های پروداکشن و استیجینگ، در صورت عدم وجود `JWT_SECRET` یا طول کمتر از ۳۲ کاراکتر، سرور بلافاصله در زمان بوت یا فراخوانی متوقف شده و خطای کشنده صادر می‌کند. ثوابت فال‌بک ناامن در پروداکشن کاملاً حذف شده‌اند.

2. **عدم امکان جعل ادمین در کلاینت:**
   - کلاینت اجازه ایجاد توکن یا نشست ادمین ساختگی بدون تایید سرور را در محیط پروداکشن ندارد. درخواست‌های ورود سریع مستقیماً به API سرور ارسال شده و در صورت عدم مجاز بودن محیط بلافاصله رد می‌شوند.

3. **هش کلمه عبور با استاندارد بالا (PBKDF2):**
   - ذخیره‌سازی گذرواژه‌ها با Salt رندوم ۱۶ بایتی کریپتوگرافیک و ۱۰۰,۰۰۰ دور اجرای الگوریتم SHA-512 انجام می‌شود.
   - مقایسه هش‌ها با متد `crypto.timingSafeEqual` برای جلوگیری از حملات زمان‌بندی (Timing Attacks) پیاده‌سازی شده است.

4. **کنترل و ریت لیمیت (Rate Limiting):**
   - تمامی مسیرهای عمومی تحت پوشش محافظت در برابر اسپم و حملات Brute-force قرار دارند.
   - ورود و ثبت‌نام دارای سقف سخت‌گیرانه برای جلوگیری از حدس مکرر رمز عبور است.

5. **خطاهای ورود بدون امکان شمارش کاربر (Non-Enumerating Login Errors & Anti-Enumeration):**
   - در کلیه مسیرهای ورود (`/api/auth/login`)، پاسخ خطا در صورت مغایرت رمز یا عدم وجود شماره تماس در سیستم، کاملاً یکسان و عمومی است (`401 INVALID_CREDENTIALS` / «شماره موبایل یا رمز عبور نادرست است.»). هیچ وضعیتی نباید وجود یا عدم وجود شماره تماس یا کاربری خاص را برای مهاجم افشا کند.
   - جهت مقابله با حملات تحلیل زمانی (Timing Attacks)، سرور در صورت عدم وجود کاربر یک هش ساختگی امن (`DUMMY_PASSWORD_HASH`) را پردازش می‌نماید تا زمان پاسخگویی کاملاً متقارن باشد.

6. **معماری آداپتورهای خنثی و عدم ادعای اتصال زنده (Provider-Neutral Adapters & No Live Claims):**
   - سامانه بوشیدو از معماری مبتنی بر درگاه‌ها و آداپتورهای خنثی بهره می‌برد (`MockSmsProvider` برای پیامک و `ProviderNeutralSimulatorAdapter` برای پرداخت اشتراک).
   - هیچ ادعایی مبنی بر اتصال زنده به ارائه‌دهندگان پیامک تجاری یا درگاه پرداخت زنده زرین‌پال وجود ندارد؛ در محیط پروداکشن بدون پیکربندی صریح اطلاعات ارائه‌دهنده توسط مدیر سیستم، سامانه در وضعیت امن Fail-Closed عمل می‌کند.

---

## ۴. چک‌لیست انتشار پروداکشن نهایی (Public Release Checklist)

قبل از عمومی کردن دامین روی لیارا یا سرور اصلی:
- [ ] متغیر `ALLOW_TEST_SHORTCUTS` حذف شود یا روی `false` قرار گیرد.
- [ ] متغیر `ENABLE_OTP_DEBUG` خاموش باشد.
- [ ] متغیر `ENABLE_QUICK_LOGIN` حذف شود.
- [ ] متغیر `JWT_SECRET` با یک کلید تصادفی پروداکشن با طول حداقل ۳۲ کاراکتر مقداردهی شود.
- [ ] متغیر `DATABASE_URL` (اتصال به دیتابیس پایدار Neon/PostgreSQL) تنظیم شده باشد.
- [ ] کلمه عبور پیش‌فرض مدیر (`SUPER_ADMIN_PASS`) در متغیرهای سرور با یک گذرواژه قوی (حداقل ۱۲ کاراکتر) تعیین گردد.
- [ ] در صورت اتصال ارائه‌دهنده پرداخت واقعی، متغیرهای مربوطه در آداپتور پرداخت تنظیم شوند (پیش‌فرض بر روی شبیه‌ساز امن/Fail-Closed قرار دارد).

---

## ۵. امنیت معماری شبیه‌سازی کاربر (Impersonation Architecture & Defense in Depth)

قابلیت ورود موقت و پشتیبانی به عنوان کاربر هدف توسط ادمین (Impersonation) تحت چارچوب امنیتی سخت‌گیرانه پیاده‌سازی شده است:

1. **ممنوعیت شبیه‌سازی مدیر توسط مدیر (Block Admin-to-Admin Impersonation):**
   - تلاش هر مدیر برای شبیه‌سازی اکانت مدیر دیگر با خطای قطعی `403 Forbidden` و کد پایدار `ADMIN_TARGET_IMPERSONATION_FORBIDDEN` مسدود می‌شود.
2. **دفاع در عمق لایه میان‌افزار (`adminMiddleware` Defense-in-Depth):**
   - تمامی روت‌های حساس مدیریتی (`/api/admin/*`) توسط میان‌افزار مرکزی بررسی می‌شوند.
   - هر توکنی با شناسه `isImpersonated: true` (بدون توجه به فیلد `isAdmin` در پی‌لود) با کد خطای `IMPERSONATION_ACCESS_FORBIDDEN` رد می‌شود. هیچ کاربر عادی، کاربر عادی شبیه‌سازی‌شده یا هدف ادمین شبیه‌سازی‌شده هرگز به پنل و روت‌های مدیریت دسترسی پیدا نخواهد کرد.
3. **خروج امن بدون شکست (Fail-Safe Exit Contract):**
   - چرخه خروج از شبیه‌سازی ابتدا توکن ذخیره‌شده ادمین را از طریق روت `/api/auth/me` یا روت اختصاصی `/api/admin/impersonate/exit` اعتبارسنجی می‌کند.
   - **دسته‌بندی قطعی پاسخ‌ها:**
     - `AUTH_REVOKED`: تنها در صورت رد قطعی احراز هویت (کد ۴۰۱، `SESSION_REVOKED`، `INVALID_TOKEN`، `USER_NOT_FOUND`). توکن‌ها پاک شده و کاربر به صفحه ورود منتقل می‌شود.
     - `INVALID_ADMIN_IDENTITY`: در صورتی که اکانت معتبر باشد اما سطح دسترسی مدیریت نداشته باشد. توکن‌ها پاک شده و خروج کامل انجام می‌شود.
     - `TEMPORARY_SERVER_ERROR`: برای خطاهای موقت سرور (۴۲۹، ۵۰۰، ۵۰۲، ۵۰۳، ۵۰۴، ۴۰۸). توکن مدیر و متادیتای شبیه‌سازی کاملاً حفظ شده، اکانت تغییر نکرده و خطای قابل‌تلاش مجدد فارسی نمایش داده می‌شود.
     - `NETWORK_ERROR`: در صورت قطع اینترنت یا خطای شبکه فچ، توکن مدیر حفظ شده و ادعای موفقیت خروج ثبت نمی‌شود.
4. **ثبت وقایع امنیتی بدون نشت اطلاعات حساس (PII-Free Audit Logging):**
   - رخدادهای ورود، رد دسترسی و خروج از شبیه‌سازی در ماژول لاگ حسابرسی ساختاریافته ثبت می‌شوند.
   - نام، شماره تماس، ایمیل یا پی‌لودهای خصوصی هرگز در لاگ‌های سیستمی ذخیره نمی‌شوند.
   - فیلد `targetUserId` در رخداد خروج (`impersonation_exited`) به عنوان متادیتای گزارش‌شده از سوی کلاینت (client-reported metadata) ثبت شده و به عنوان وضعیت سرورمحور تلقی نمی‌گردد.

---

## ۶. ردیابی عملیاتی و مصونیت داده‌ها در لاگ‌های سرور (Operational Observability & Redaction Invariant)

1. **شناسه سرورمحور درخواست (`X-Request-ID`):**
   - تمامی درخواست‌ها دارای شناسه یکتای سرورمحور کریپتوگرافیک هستند.
   - کلاینت اجازه تحمیل یا دستکاری شناسه سرور را ندارد. شناسه کلاینت صرفاً به عنوان متادیتای دریافتی تفکیک‌شده ثبت می‌شود.
2. **اصل مصونیت از ثبت داده‌های خصوصی (Zero-PII & Redaction Policy):**
   - بدنه درخواست‌ها (`request body`)، کوئری استرینگ‌ها، هدرها، توکن‌های JWT، کوکی‌ها، گذرواژه‌ها و کدهای یک‌بار مصرف OTP هرگز در لاگ‌های سرور درج نمی‌شوند.
   - در صورت رخ دادن خطای ۵۰۰ غیرمنتظره، پیام خطا توسط فیلتر پالایشگر عبور داده شده و تمامی کلیدهای محیطی (`JWT_SECRET`, `DATABASE_URL`, `SUPER_ADMIN_PASS`, `ZARINPAL_MERCHANT_ID`) به صورت قطعی سانسور (`[REDACTED]`) می‌گردند.

---

## ۷. بدهی امنیتی موکول‌شده به فازهای آتی (Deferred Security & Release Hardening Debt)

- **ذخیره‌سازی توکن حامل در حافظه جاوااسکریپت (JavaScript-Accessible Bearer-Token Storage):**
  - **وضعیت فعلی:** توکن‌های احراز هویت و شبیه‌سازی در `localStorage` و `sessionStorage` نگهداری می‌شوند که در دسترس جاوااسکریپت کلاینت قرار دارد.
  - **ریسک مرتبط:** در صورت بروز آسیب‌پذیری احتمالی تزریق اسکریپت (XSS)، امکان دستیابی به توکن احراز هویت وجود دارد.
  - **برنامه ارتقا در فاز سخت‌سازی:** مهاجرت به کوکی‌های امن سرورمحور با پرچم‌های `HttpOnly`, `Secure`, `SameSite=Strict` به همراه محافظت ضد جعل درخواست (CSRF Token / Origin Verification) برای حذف کامل دسترسی مستقیم اسکریپت به توکن‌های سشن.

---

## ۸. چارچوب امنیت حذف حساب و چرخه حیات داده‌ها (Data Retention & Account Deletion Contract)

قرارداد جامع امنیت حذف حساب کاربری و نگهداری داده‌ها در سند [`docs/DATA_RETENTION_AND_ACCOUNT_DELETION.md`](./docs/DATA_RETENTION_AND_ACCOUNT_DELETION.md) تعریف و تثبیت شده است:

1. **احراز هویت مجدد الزامی (Mandatory Reauthentication):** درخواست حذف حساب نیازمند ارسال مجدد کلمه عبور معتبر یا کد چالش تایید شده OTP با هدف `ACCOUNT_DELETION` است.
2. **ممنوعیت حذف از طریق نشست شبیه‌سازی (Impersonation Deletion Prohibited):** توکن‌های دارای پرچم `isImpersonated: true` اجازه حذف حساب کاربری هدف را ندارند (`403 IMPERSONATION_DELETION_FORBIDDEN`).
3. **مصونیت حساب‌های مدیریت ارشد (Admin Deletion Protection):** حساب‌های ادمین ارشد از طریق اندپوینت‌های عمومی قابل حذف نیستند (`403 ADMIN_DELETION_PROHIBITED`).
4. **تراکنش اتمیک و Fail-Closed:** عملیات حذف دیتابیس درون یک تراکنش واحد (`prisma.$transaction`) اجرا شده و در صورت بروز خطا بدون ایجاد رکوردهای یتیم Rollback می‌گردد.
5. **پاکسازی کامل حافظه محلی و صف‌های آفلاین:** در کلاینت کلیه کلیدهای `localStorage`، صف‌های جهش و داده‌های قرنطینه پاکسازی می‌شوند تا از احیای داده‌های حذف‌شده جلوگیری گردد.




