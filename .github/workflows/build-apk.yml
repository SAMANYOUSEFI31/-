name: Build Android APK

# STATUS: DISABLED (BLOCKER: Tracked Native Android Architecture Missing)
#
# Blocker Assessment:
# The repository contains capacitor.config.json but lacks an authoritative, tracked
# `android/` native project tree (including Gradle wrapper, build.gradle, and AndroidManifest.xml).
# Generating Android architecture dynamically at CI runtime via `cap add android` with unpinned
# runtime package installation and skipped validation violates Phase 2C.6 supply-chain and
# deterministic build invariants.
#
# Resolution Requirement:
# This workflow is explicitly disabled from automated CI execution until native Android project
# assets are baselined, tracked, validated, and declared in package.json/package-lock.json.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-android:
    name: Build Android APK (Disabled - Missing Tracked Android Project)
    runs-on: ubuntu-24.04
    permissions:
      contents: read

    steps:
      - name: Report Blocker Status
        run: |
          echo "===================================================================================="
          echo "Android APK build is disabled due to missing tracked native Android architecture."
          echo "Tracked 'android/' directory and declared Capacitor runtime packages are required."
          echo "See RUNBOOK.md and SECURITY.md for details."
          echo "===================================================================================="
          exit 1
