# 🛠️ کتابچه عملیاتی و دیباگ (Operational Runbook & Troubleshooting)

> **مخاطب:** تیم فنی، DevOps و پشتیبانی سیستم  
> **سامانه:** Bushido Discipline OS

---

## ۱. ارزیابی سلامت سامانه (`GET /api/health`)

سریع‌ترین روش برای بررسی وضعیت زنده برنامه، فراخوانی آدرس `/api/health` است:

### نمونه پاسخ سالم (PostgreSQL فعال):
```json
{
  "status": "ok",
  "engine": "Bushido Discipline OS (Production Grade)",
  "mode": "production",
  "version": "3.0.0",
  "uptimeSeconds": 3600,
  "timestamp": "2026-09-01T14:22:00.000Z",
  "nodeVersion": "v22.x",
  "memoryRssMb": 85,
  "database": {
    "driver": "postgresql_prisma",
    "isPrismaAvailable": true,
    "isServerlessVercel": true
  },
  "security": {
    "testShortcutsEnabled": false,
    "otpDebugEnabled": false
  }
}
```

### نمونه پاسخ در حالت Fallback محلی:
```json
{
  "database": {
    "driver": "local_file_fallback",
    "isPrismaAvailable": false,
    "isServerlessVercel": true
  }
}
```
> **تحلیل:** اگر `driver` برابر با `local_file_fallback` باشد، یعنی دیتابیس PostgreSQL متصل نشده و برنامه از فایل موقت حافظه استفاده می‌کند. برای رفع این موضوع، متغیر `DATABASE_URL` را در Vercel بررسی کنید.

---

## ۲. سناریوهای عیب‌یابی متداول (Troubleshooting Matrix)

| نشانه / خطا | علت احتمالی | اقدام اصلاحی |
| :--- | :--- | :--- |
| **دکمه‌های ورود تستی در پروداکشن کار نمی‌کنند** | رفتار قطعی و غیرقابل تغییر امنیتی (Fail-Closed) | ورود سریع، میانبرهای تستی، OTP ساختگی و پرداخت تستی در پروداکشن کاملاً غیرممکن هستند و متغیر `ALLOW_TEST_SHORTCUTS` در پروداکشن نادیده گرفته می‌شود. برای ورود ادمین از رمز عبور واقعی سوپرادمین استفاده کنید. |
| **خطای ۵۰۰ یا کرش سرور در لاگین پروداکشن (`JWT_SECRET`)** | عدم تنظیم متغیر `JWT_SECRET` یا طول کمتر از ۳۲ کاراکتر | در پروداکشن هیچ کلید پیش‌فرضی وجود ندارد (Fail-Closed). یک کلید تصادفی با حداقل ۳۲ کاراکتر در متغیر `JWT_SECRET` قرار دهید. |
| **عدم امکان ورود سوپرادمین در پروداکشن** | عدم تنظیم متغیرهای سوپرادمین | در پروداکشن هیچ نام کاربری یا گذرواژه پیش‌فرضی وجود ندارد. متغیرهای `SUPER_ADMIN_PHONE` (یا `SUPER_ADMIN_EMAIL`) و `SUPER_ADMIN_PASS` را تنظیم کنید یا از طریق دیتابیس نقش ادمین بدهید. |
| **اعمال نشدن تغییرات اسکیما در دیتابیس پس از استقرار** | اجرای بیلد ساختار دیتابیس را دستکاری نمی‌کند | بیلد نرم‌افزار هرگز نباید اسکیما را تغییر دهد. مایگریشن‌ها صرفاً از طریق اجرای دستور `npm run db:migrate:deploy` اعمال می‌شوند. |
| **لاگ‌های کاربر پس از ری‌استارت Vercel از نو می‌شوند** | اتصال دیتابیس به PostgreSQL انجام نشده و سرورلس فایل موقت `/tmp` را خالی کرده است | متغیر `DATABASE_URL` را به یک سرویس پایدار (مثل Neon Postgres) متصل کنید و دستور `npm run db:migrate:deploy` را اجرا نمایید. |
| **خطای CORS یا خطای ۵۰۰ در ثبت نام** | اشتباه در مقدار متغیرهای احراز هویت | لاگ‌های سرور (Runtime Logs) را در پنل Vercel بررسی کنید. |
| **نسخه جدید UI برای کاربر نمایش داده نمی‌شود (کش PWA)** | سرویس‌ورکر نسخه قدیمی را کش کرده است | از منوی تنظیمات یا کلید میانبر، کش مرورگر کاربر را نوسازی نمایید یا کش سرویس‌ورکر را در تب Application مرورگر Unregister کنید. |

---

## ۳. مرزهای ایمنی پروداکشن (Production Safety Boundaries)

1. **عدم دستکاری دیتابیس در فرآیند بیلد:**
   - فرآیند `build` و `vercel-build` هرگز نباید ساختار دیتابیس را جهش دهند یا تغییر دهند (`prisma db push` به طور کامل حذف شده است).
   - مایگریشن‌های اسکیما فقط و فقط از طریق `npm run db:migrate:deploy` به صورت کنترل‌شده و ایمن در پروداکشن اعمال می‌گردند.
2. **مدل شکست-بسته در پروداکشن (Production Fails Closed):**
   - هرگونه وضعیت ناشناخته یا عدم تطابق محیطی به عنوان `production` تفسیر می‌شود و سیستم سریعاً قفل می‌گردد.
3. **غیرممکن بودن میانبرها و شبیه‌سازها در پروداکشن:**
   - تست شورتکات‌ها، ورود سریع (Quick Login)، پیامک ساختگی (Mock OTP)، دیباگ OTP در پاسخ‌های API و پرداخت شبیه‌سازی‌شده (Mock Payment) در محیط پروداکشن تحت هر شرایطی غیرممکن و مسدود هستند؛ حتی اگر متغیر `ALLOW_TEST_SHORTCUTS=true` اشتباهاً در پروداکشن ست شده باشد.
4. **حذف اعتبارنامه‌های پیش‌فرض سوپرادمین:**
   - در پروداکشن هیچ کاربر پیش‌فرض یا گذرواژه هاردکدشده‌ای برای سوپرادمین وجود ندارد. دسترسی ادمین منوط به تنظیم صریح متغیرهای اختصاصی مدیر ارشد است.
5. **حذف قطعی کلید فال‌بک JWT:**
   - متغیر `JWT_SECRET` در پروداکشن فاقد هرگونه مقدار پیش‌فرض است و باید حداقل ۳۲ کاراکتر داشته باشد؛ در غیر این صورت سیستم Fail-Closed عمل کرده و به هیچ عنوان توکن نامعتبر صادر یا تایید نمی‌کند.

---

## ۴. بررسی لاگ‌های سرور (Runtime Logs)

کدهای لاگ با برچسب‌های استاندارد در خروجی کنسول ثبت می‌شوند:
- `[Database] PostgreSQL connected via Prisma datasource`: اتصال موفق به Postgres.
- `[Database] Running in self-hosted persistent file/memory database mode`: فعال بودن موتور پشتیبان محلی.
- `[Bushido Auth]`: ثبت‌نام و نشست‌های کاربران.
- `[RateLimit]`: مسدودسازی آی‌پی‌های ارسال‌کننده ریکوئست‌های بیش از حد مجاز.

---

## ۴. اهداف و بهینه‌سازی‌های آینده [هدف آینده]

- [هدف آینده] اضافه کردن لایه Web Push Notifications برای یادآوری زمان کات‌آف شبانه.
- [هدف آینده] مهاجرت کامل وب‌سوکت برای همگام‌سازی لحظه‌ای چنددستگاهی در سطح حساب‌های VIP.
